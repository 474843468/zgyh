<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <TextView
        android:id="@+id/tv_title_msg"
        style="@style/textview_common"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/epay_bom_service_open_account_select_tv_title"
        android:textColor="@color/gray" />

    <ListView
        android:id="@+id/acc_select_listview"
        style="@style/listviewcomm_wrap_haveDivider"
        android:layout_above="@+id/bt_next"
        android:layout_alignParentTop="@dimen/common_row_margin"
        android:layout_below="@id/tv_title_msg"
        android:layout_marginBottom="@dimen/common_row_margin" >
    </ListView>

    <Button
        android:id="@+id/bt_next"
        style="@style/bottombtn"
        android:layout_alignParentBottom="true"
        android:layout_marginTop="@dimen/common_row_margin"
        android:text="@string/next" />

</RelativeLayout>